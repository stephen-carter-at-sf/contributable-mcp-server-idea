{
    "name": "@somenamespace/example-mcp-provider",
    "version": "0.0.1-alpha",
    "type": "module",
    "main": "dist/index.js",
    "dependencies": {
        "@somenamespace/mcp-provider-api": "0.0.1-alpha",
        "zod": "^3.23.8"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "rimraf": "^6.0.1"
    },
    "files": [
        "dist",
        "LICENSE",
        "package.json"
    ],
    "scripts": {
        "build": "tsc --build tsconfig.build.json --verbose",
        "clean": "tsc --build tsconfig.build.json --clean",
        "postclean": "rimraf dist && rimraf coverage && rimraf ./*.tgz",
        "package": "npm pack",
        "test": "echo 'need to add test framework script here'"
    }
}
